{
  "name": "@el-jardin/dominio",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "biome check .",
    "type-check": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"Sin tests en @el-jardin/dominio aun\"",
    "format": "biome check . --write"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "typescript": "^5.9.3"
  }
}
